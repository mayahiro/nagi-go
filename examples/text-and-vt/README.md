# Text and VT

This example measures terminal Cell width with Nagi Text and constructs a
typed terminal Style with Nagi VT

Run it from the Go repository root:

```sh
go run ./examples/text-and-vt
```

It prints `width=4 bold=true`
